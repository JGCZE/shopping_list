/* import React from "react";

const Button = ({ children }: { children: React.ReactNode }) => (
  <button className="border-2 px-10 py-4 font-bold cursor-pointer">
    {children}
  </button>
);

export default Button;
 */
